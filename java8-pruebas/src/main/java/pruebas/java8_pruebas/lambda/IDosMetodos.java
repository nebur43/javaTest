package pruebas.java8_pruebas.lambda;

/**
 * imposible hacer una funcion lambda con esto, puesto que solo se puede hacer
 * con interfaces funcionales: es una interfaz con uno y solo un método abstracto
 * 
 * @author 67760769 - Rubén Aguado
 *
 */
// @FunctionalInterface
public interface IDosMetodos {

	public boolean test(boolean in) ;
	
	public String test2(String s) ;
	
}
